---
name: Update Demo GIF

'on':
  push:
    branches:
      - main
    paths:
      - demo.tape
      - .github/workflows/update-demo.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-demo:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout code
        # v6.0.1
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8

      - name: Setup mise
        # v3.5.1
        uses: jdx/mise-action@146a28175021df8ca24f8ee1828cc2a60f980bd5
        with:
          github_token: ${{ github.token }}

      - name: Install dependencies
        run: npm ci

      - name: Build project
        run: npm run build

      - name: Link CLI for demo
        run: npm link

      - name: Generate demo GIF
        # v1.3.0
        uses: charmbracelet/vhs-action@1b9acfe3b952e00ff2c15933b84091fc5f33b6e7
        with:
          path: demo.tape

      - name: Commit and push updated demo.gif
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"

          if [[ -n $(git status --porcelain media/demo.gif) ]]; then
            git add media/demo.gif
            git commit -m "chore: update demo.gif [skip ci]"
            git push
          else
            echo "No changes to demo.gif"
          fi
